{
  "name": "liist.me",
  "version": "1.0.0",
  "description": "liist.me allows users to organize a collaborative playlist of music for any function.",
  "main": "server.js",
  "scripts": {
    "test": "mocha",
    "start": "npm-run-all --parallel start-server start-mockapi",
    "start-server": "node server.js",
    "generate-mock-liist-data": "node mockAPI/generateMockLiistData",
    "prestart-mockapi": "npm run generate-mock-liist-data",
    "start-mockapi": "json-server --watch mockAPI/db.json --port 3001"
  },
  "author": "Jaiwoo",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-http": "^4.0.0",
    "chalk": "^2.4.1",
    "faker": "^4.1.0",
    "json-schema-faker": "^0.5.0-rc15",
    "json-server": "^0.14.0",
    "mocha": "^5.2.0",
    "npm-run-all": "^4.1.3"
  }
}
